<UserControl
    x:Class="SchoolJournal.Desktop.Features.Core.StudentSubgroups.StudentSubgroupsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel
            Grid.Row="0"
            Margin="0,0,0,15"
            Orientation="Horizontal">
            <TextBlock
                FontSize="18"
                FontWeight="Bold"
                Text="Керування складом підгруп" />
        </StackPanel>

        <Border
            Grid.Row="1"
            Margin="0,0,0,15"
            Padding="15"
            Background="#ECF0F1"
            CornerRadius="5">
            <StackPanel Orientation="Horizontal">
                <TextBlock
                    Margin="0,0,10,0"
                    VerticalAlignment="Center"
                    Text="Клас:" />
                <ComboBox
                    Width="150"
                    Margin="0,0,30,0"
                    Padding="5"
                    VerticalAlignment="Center"
                    DisplayMemberPath="ClassName"
                    ItemsSource="{Binding AvailableClasses}"
                    SelectedItem="{Binding SelectedClass}" />

                <TextBlock
                    Margin="0,0,10,0"
                    VerticalAlignment="Center"
                    Text="Підгрупа:" />
                <ComboBox
                    Width="200"
                    Padding="5"
                    VerticalAlignment="Center"
                    DisplayMemberPath="SubgroupName"
                    ItemsSource="{Binding AvailableSubgroups}"
                    SelectedItem="{Binding SelectedSubgroup}" />
            </StackPanel>
        </Border>

        <StackPanel
            Grid.Row="2"
            Margin="0,0,0,15"
            Orientation="Horizontal"
            Visibility="{Binding IsAdminOrDirector, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock
                Margin="0,0,10,0"
                VerticalAlignment="Center"
                Text="Додати студента:" />
            <ComboBox
                Width="250"
                Margin="0,0,10,0"
                Padding="5"
                VerticalAlignment="Center"
                ItemsSource="{Binding AvailableStudents}"
                SelectedItem="{Binding SelectedAvailableStudent}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>
                            <Run Text="{Binding LastName}" />
                            <Run Text="{Binding FirstName}" />
                        </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button
                Padding="15,5"
                Background="#2ECC71"
                Command="{Binding AssignStudentCommand}"
                Content="Додати"
                Foreground="White" />
        </StackPanel>

        <TextBlock
            Grid.Row="3"
            Margin="0,10,0,10"
            Foreground="#E74C3C"
            Text="{Binding ErrorMessage}"
            Visibility="{Binding ErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <DataGrid
            Grid.Row="4"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            IsReadOnly="True"
            ItemsSource="{Binding Students}"
            SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding LastName}"
                    Header="Прізвище" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding FirstName}"
                    Header="Ім'я" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Patronymic}"
                    Header="По батькові" />
                <DataGridTemplateColumn Width="Auto" Header="Дії">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button
                                Margin="2"
                                Padding="10,2"
                                Background="#E74C3C"
                                Command="{Binding DataContext.RemoveStudentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                CommandParameter="{Binding}"
                                Content="Видалити"
                                Foreground="White"
                                Visibility="{Binding DataContext.IsAdminOrDirector, RelativeSource={RelativeSource AncestorType=DataGrid}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <ProgressBar
            Grid.Row="4"
            VerticalAlignment="Top"
            IsIndeterminate="True"
            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />
    </Grid>
</UserControl>